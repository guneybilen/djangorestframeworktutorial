version: "3"

services:
  app:
    build:
      context: .
    volumes:
      - .:/app
    ports:
      - "7000:7000"
    command: |
      sh -c "python /app/manage.py migrate &&
             python /app/manage.py runserver 0.0.0.0:7000"